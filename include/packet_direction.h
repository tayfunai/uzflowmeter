#ifndef PACKET_DIRECTION_H
#define PACKET_DIRECTION_H

// PacketDirection Enum in C
typedef enum {
    FORWARD = 1,  // Forward direction
    REVERSE = 2,   // Reverse direction
    BOTH = 3
} PacketDirection;

#endif /* PACKET_DIRECTION_H */
